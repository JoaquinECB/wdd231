import { setupNavigation, setupModal, displayFormResponse } from './modules.js';

document.addEventListener('DOMContentLoaded', () => {
    setupNavigation();
    setupModal();
    displayFormResponse();
});